import { YourdirectiveDirective } from './yourdirective.directive';

describe('YourdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new YourdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
